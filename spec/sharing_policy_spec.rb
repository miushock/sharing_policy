require "spec_helper"

describe SharingPolicy do

  let(:user) {double}
  
  describe ".viewing_policy" do
    it "returns an viewving policy instance when given valid json spec" do
    end
    
    it "raise parsing exception when failed to read or validate givine json" do
    end
  end

  describe ".pulling_policy" do
    it "returns an pulling policy instance when given valid json spec" do
    end
    
    it "raise parsing exception when failed to read or validate givine json" do
    end
  end
end

